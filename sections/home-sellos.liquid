<div class="py-12 bg-[#4a4a49] text-white my-8 lg:my-12">
  <div class="wrapper">
    <ul class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      {% for block in section.blocks %}
        <li class="flex items-center text-center flex-col relative">
          {% if block.settings.link != blank %}
          <a href="{{ block.settings.link }}" class="absolute top-0 left-0 block w-full h-full"></a>  
          {% endif %}
          <img
            src="{{ block.settings.image | img_url: '150x' }}"
            alt="{{ block.settings.title | escape }}"
            width="150"
            height="{{ 150 | divided_by: block.settings.title.aspect_ratio | ceil }}"
            loading="lazy"
            class="mb-4">
          <h3 class="mb-2 uppercase font-extrabold text-base text-center">{{ block.settings.title | escape }}</h3>  
          <p class="text-base">
            {{ block.settings.content | escape }}
          </p>
        </li>
      {% endfor %}
    </ul>
  </div>
</div>
{% schema %}
  {
    "name": "Home Sellos",
    "max_blocks": 3,
    "blocks": [
      {
        "name": "Sello",
        "type": "sello",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Inserta el icono"
          }, {
            "type": "text",
            "id": "title",
            "label": "Inserta el título",
            "default": "Lorem, ipsum dolor."
          }, {
            "type": "text",
            "id": "content",
            "label": "Inserta el contenido",
            "default": "Lorem ipsum dolor, sit amet consectetur adipisicing elit. Maiores nesciunt accusamus error quae amet voluptatibus reprehenderit vero autem quasi in!"
          }, {
            "type": "url",
            "id": "link",
            "label": "Link del icono."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Home Sellos"
      }
    ]
  }
{% endschema %}